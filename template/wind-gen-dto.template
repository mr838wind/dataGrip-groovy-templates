package $packageNameDTO

import lombok.Data;

/**
 * ${className}DTO
 */

@Data
public class ${className}DTO {

    <%fields.each() {%>
    /**
     * ${it.comment}
     * column: ${it.dbName}
     */
     private ${it.type} ${it.name};
    <%}%>

}